#ifndef KEYBOARD_INPUT_H
#define KEYBOARD_INPUT_H

enum class keyboardInput
{
    press_esc = 0,
    press_enter,
    close_window,
    press_up,
    press_down,
    press_space,
    release_space
};

#endif